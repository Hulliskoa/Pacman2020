#pragma once
#include <vector>
#include <SDL.h>

class LevelManager
{
private:
	//int currentLevel;
	//int levels[][][];
public:
	//LevelManager(std::shared_ptr<CollisionManager> collisionManager)
	//void renderLevel();
	//void createLevel();
	//void update(SDL_Renderer* renderer);
};

